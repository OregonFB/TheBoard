{
  "name": "the-board",
  "version": "1.0.0",
  "private": true,
  "description": "The Board — Oregon Football recruiting war room (PWA)",
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --minify --outfile=docs/app.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
