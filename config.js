/* The Board — deployment config.
 *
 * OUT OF THE BOX: the board saves to each device (localStorage). Fine for demos.
 *
 * SHARED STAFF BOARD (recommended): create a free project at supabase.com,
 * run supabase/schema.sql in its SQL editor, then paste your project's
 * URL and anon public key below and commit this file.
 *
 * NOTE: the anon key is public by design (it ships to every browser).
 * Anyone who has it can read/write the board — same trust level as the
 * shared staff password. Do not store sensitive data until real
 * authentication is added.
 */
window.BOARD_CONFIG = {
  // supabase: {
  //   url: "https://YOUR-PROJECT.supabase.co",
  //   anonKey: "YOUR-ANON-PUBLIC-KEY",
  // },
};
